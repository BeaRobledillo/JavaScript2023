//12.Reemplazar una palabra en un string:

//El método replace() devuelve una nueva cadena con algunas o todas las coincidencias de un patrón, 
//siendo cada una de estas coincidencias reemplazadas por remplazo.

let myString = 'All Right!';
console.log(myString);
console.log(myString.replace('All', 'NO'));
console.log("⚡".split(" "));
