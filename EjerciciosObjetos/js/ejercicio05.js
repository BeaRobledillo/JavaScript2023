//5.Crear un método para un objeto que devuelva una lista de todas las claves:

let alumno={name:'Luis', edad:22, nota:5, apobado:false};

console.log(Object.keys(alumno));

//gema:
/* const objeto1 = {
    nombre: "María",
    alumno: "127",
};
console.log(Object.keys(objeto1));
 */